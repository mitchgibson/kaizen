# Kaizen Plugin — Quick Start\n\nGet the Kaizen plugin built and running in under 5 minutes.\n\n## Prerequisites\n\n- Node.js 18+ installed\n- Obsidian vault set up\n- This plugin directory: `~/.obsidian/plugins/obsidian-sample-plugin/`\n\n## Step 1: Install Dependencies\n\n```bash\ncd ~/.obsidian/plugins/obsidian-sample-plugin\nnpm install\n```\n\n## Step 2: Build the Plugin\n\n### Development (Watch Mode)\n\n```bash\nnpm run dev\n```\n\nLeaves terminal running and rebuilds on file changes.\n\n### Production Build\n\n```bash\nnpm run build\n```\n\nOne-time build of optimized code.\n\n## Step 3: Enable in Obsidian\n\n1. Close and reopen Obsidian (or reload with `Cmd+R` / `Ctrl+Shift+R`)\n2. Settings → Community Plugins → Enable \"Kaizen Habits\"\n3. You should see a checkmark icon in the left ribbon\n\n## Step 4: Create Your First Habit\n\n1. Press `Cmd/Ctrl + P` (Command Palette)\n2. Search: \"Create New Habit\"\n3. Enter name: \"Morning Exercise\"\n4. ✅ Habit file created at `Habits/Morning Exercise.md`\n\n## Step 5: Try It Out\n\n### Open Sidebar\n- Click the checkmark icon in the ribbon, OR\n- Command Palette → \"Open Kaizen Sidebar\"\n\n### Mark Habit Done\n- Click the ◯ button next to your habit\n- It turns ✅ and count increments\n\n### View Analytics\n- Command Palette → \"Open Kaizen Analytics\"\n- See detailed stats: streak, count, 30-day %, etc.\n\n## File Structure\n\nAfter building, you'll have:\n\n```\nobsidian-sample-plugin/\n├── src/                    # Source code (TypeScript)\n├── main.js                 # Compiled plugin (auto-generated)\n├── manifest.json           # Plugin metadata\n├── styles.css              # Styles\n├── package.json            # Dependencies\n└── KAIZEN_README.md        # Full documentation\n```\n\n## Common Tasks\n\n### Rebuild After Changes\n\nIf running `npm run dev`, changes auto-rebuild. Otherwise:\n\n```bash\nnpm run build\n```\n\nThen reload Obsidian (`Cmd+R` / `Ctrl+Shift+R`).\n\n### Check for Errors\n\n```bash\nnpm run lint  # ESLint\n```\n\n### Test Domain Logic\n\nDomain functions are pure and testable:\n\n```typescript\n// In Node.js\nimport { currentStreak, markDone } from './src/domain/habit';\n\nconst h = { id: '1', title: 'Run', history: ['2025-01-01', '2025-01-02', '2025-01-03'] };\nconst h2 = markDone(h, '2025-01-04');\nconsole.log(currentStreak(h2)); // 4\n```\n\n## Settings\n\nSettings → Kaizen:\n\n- **Auto-log to Daily Note**: OFF by default (enable if you want entries in daily notes)\n- **Derive Count from History**: ON (calculates count from history array)\n- **Habit Field Name**: `habit` (frontmatter field to identify habits)\n- **Developer Logging**: OFF (enable to see debug output)\n\n## Creating Habits Manually\n\nCreate a file `Habits/MyHabit.md`:\n\n```yaml\n---\ntitle: My Habit\nhabit: true\ntrigger: After breakfast\nschedule: daily\nhistory: []\n---\n\n# My Habit\n\nDetails about this habit.\n```\n\nIt will appear in the sidebar instantly.\n\n## Troubleshooting\n\n### \"No habits yet\" message\n\n- Create a habit with \"Create New Habit\" command\n- Or manually create a file with `habit: true` in frontmatter\n- Ensure it's in a `.md` file\n\n### Changes not appearing\n\n- Kill `npm run dev` if running\n- Run `npm run build`\n- Reload Obsidian (`Cmd+R` / `Ctrl+Shift+R`)\n\n### Build errors\n\n```bash\nrm -rf node_modules\nnpm install\nnpm run build\n```\n\n### Check console for errors\n\n- Obsidian: Settings → About → Console\n- Browser DevTools: Right-click → Inspect → Console tab\n\n## Next Steps\n\n1. **Create 2-3 habits** and mark them done daily\n2. **Explore analytics** view to see streaks and stats\n3. **Read** `KAIZEN_README.md` for full features\n4. **Customize** in Settings if desired\n5. **Develop further** by modifying `src/` files (auto-rebuilds with `npm run dev`)\n\n## Documentation\n\n- **KAIZEN_README.md**: Complete user guide\n- **IMPLEMENTATION_COMPLETE.md**: Architecture and design decisions\n- **.design/implementation-plan.md**: Original specification\n\n## Commands\n\n- `npm run dev` — Watch mode (auto-rebuild)\n- `npm run build` — Production build\n- `npm run lint` — ESLint check\n\n## Support\n\nIf stuck:\n\n1. Check `KAIZEN_README.md` troubleshooting section\n2. Enable \"Developer Logging\" in settings\n3. Look at browser console for errors\n4. Check source code comments in `src/` directory\n\n---\n\n**Ready? Build it! 🚀**\n\n```bash\nnpm run dev\n```\n\nThen open Obsidian and enjoy your Kaizen habit tracker.\n"